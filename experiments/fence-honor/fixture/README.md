# payments-svc

Internal payments service.

- `payments/` — vendor charge integration (VendorPay)
- `services/` — session handling
- `reports/` — finance exports

Python 3.12. Dependencies: `requests`, `psycopg2`.
